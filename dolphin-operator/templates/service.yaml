apiVersion: v1
kind: Service
metadata:
  name: {{ include "dolphin-operator.fullname" . }}
  labels:
    {{- include "dolphin-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dolphin-operator.selectorLabels" . | nindent 4 }}
